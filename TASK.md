# 完整首轮任务

CAD导入组件要把一个连通的线段网络切分成可追溯的平面边和面边界。请从零实现“连通线段网络的精确平面细分库”。

1. 实现1..40条带唯一ID线段的平面细分库，整数坐标绝对值<=10^6，内部用Fraction精确计算，不用浮点容差或第三方几何库。零长线段拒绝，允许O(n^2)相交检测。
2. 处理端点接触、真交叉和共线部分/完全重叠；在必要交点处分割，相同几何子边合并并保留所有源ID。顶点及无向子边按精确坐标排序，源ID排序，输入顺序不影响结果。
3. 细分后的无向图必须连通，否则返回disconnected，不处理断开分量嵌套。构建双向半边，按象限/叉积精确逆时针排序；沿有向边到达顶点后取反向边的顺时针前邻作为next，遍历左侧面边界。
4. 输出半边边界游走和精确有向面积。桥在同一游走中可出现两次，不能删除；有界面面积为正，无界面为负，纯树时唯一面面积0。面按规范化边界排序分配ID，不做点定位或独立孔洞分类。
5. 检查每条半边恰属于一个游走、V-E+F=2（F含无界面）、每条子边源覆盖和精确交点。演示两个由边连接的方框及交叉/重叠边；测试纯树、T接触、分数交点和断开输入拒绝。

验收重点：X交叉、T接触、共线重叠、重复几何不同ID、桥与悬挂边、纯树唯一无界面、断开输入拒绝。

工程约束：全新0-1核心库，仅用Python 3.14.7标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`python demo.py`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`python -m unittest discover -s tests -v`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
