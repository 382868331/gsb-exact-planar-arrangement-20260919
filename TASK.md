# 完整首轮任务

给定一个连通的线段网络，需要求出交点、拆分后的边和各个面。请从零实现精确的平面细分库，使用有理数处理坐标，具体输入范围和拓扑约定如下。

实现1..40条带唯一ID线段的平面细分库，整数坐标绝对值<=10^6，内部用Fraction精确计算，不用浮点容差或第三方几何库。零长线段拒绝，允许O(n^2)相交检测。

处理端点接触、真交叉和共线部分/完全重叠；在必要交点处分割，相同几何子边合并并保留所有源ID。顶点及无向子边按精确坐标排序，源ID排序，输入顺序不影响结果。

细分后的无向图必须连通，否则返回disconnected，不处理断开分量嵌套。构建双向半边，按象限/叉积精确逆时针排序；沿有向边到达顶点后取反向边的顺时针前邻作为next，遍历左侧面边界。

输出半边边界游走和精确有向面积。桥在同一游走中可出现两次，不能删除；有界面面积为正，无界面为负，纯树时唯一面面积0。面按规范化边界排序分配ID，不做点定位或独立孔洞分类。

检查每条半边恰属于一个游走、V-E+F=2（F含无界面）、每条子边源覆盖和精确交点。演示两个由边连接的方框及交叉/重叠边；测试纯树、T接触、分数交点和断开输入拒绝。

测试里请覆盖这些情况：X交叉、T接触、共线重叠、重复几何不同ID、桥与悬挂边、纯树唯一无界面、断开输入拒绝。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
