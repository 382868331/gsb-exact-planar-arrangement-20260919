# 完整首轮任务

请从零实现“精确线段平面细分库”，用于计算几何 / CAD 内核中的本地可复用模块。

1. 从零实现二维线段平面细分库，输入至多80条带唯一ID的线段，端点为有界整数，内部用 Fraction 精确计算，不用浮点容差或几何第三方库。零长线段拒绝。允许O(n^2)交点检测，不要求扫描线优化。
2. 处理端点接触、真交叉和共线部分/完全重叠；在所有必要交点处分割，将相同几何子边合并，并保存覆盖该子边的全部源线段ID。顶点和子边按精确坐标规范排序。
3. 构建双向半边，使用叉积与象限进行精确角排序，建立下一半边并遍历面边界；输出有向边界游走、精确有向面积及其所属面。桥边可能在同一面出现两次，不能误报为有面积面。
4. 将不同连通分量的嵌套边界正确归属于同一个面，区分外部无界面、独立内环和孔洞，不能把每次半边游走当作一个独立面；可用精确点包含与内部采样判定，说明边界点处理。
5. 提供查询某点位于顶点/边/哪个面；验证每条半边恰属于一个边界、V-E+F=1+C（F包含无界面）、源覆盖保真及输入顺序无关。演示带交叉线与嵌套环的小案例。

验收重点：十字交点、T接触、重叠线段、重复几何不同ID、桥与悬挂边、不连通方框、嵌套孔洞、精确分数交点。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
